#include"main.h"
#include<string.h>
#include<stdio.h>

/**
 * _strlen - This is the name for the function
 * @s: This is the string
 * Return: (l)
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != 0; i++)
	{
	}
	return (i);
}
