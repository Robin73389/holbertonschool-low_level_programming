#include"main.h"
#include<string.h>
#include<stdio.h>

/**
 * _strlen - This is the name for the function
 * @s: This is the string
 * Return: (l)
 */
int _strlen(char *s)
{
	int l;

	l = strlen(s);

	return (l);
}
